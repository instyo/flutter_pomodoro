import 'dart:ui';

class TimerData {
  final Color color;
  final int maxTime;
  final String label;

  const TimerData({
    required this.color,
    required this.maxTime,
    required this.label,
  });
}
